apiVersion: v1
kind: Namespace
metadata:
  name: ns-name

---
#Create a LimitRange and a Pod
apiVersion: v1
kind: LimitRange
metadata:
  name: mem-limit-range
spec:
  limits:
    - default:
        memory: 512Mi
      defaultRequest:
        memory: 256Mi
      type: Container